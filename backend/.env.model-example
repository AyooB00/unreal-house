# Model Configuration Example
# Copy this to .env to override the default model

# Default model is now gpt-4o-mini
# You can override it if needed:

# Use GPT-4 Turbo for highest quality (more expensive)
# OPENAI_MODEL=gpt-4-turbo-preview

# Use GPT-4o-mini (default - best balance of quality and cost)
# OPENAI_MODEL=gpt-4o-mini

# Use GPT-3.5 Turbo (older, actually more expensive than 4o-mini)
# OPENAI_MODEL=gpt-3.5-turbo

# Cost comparison (per 1M tokens):
# - gpt-4o-mini: $0.15 input / $0.60 output (CHEAPEST & BEST)
# - gpt-3.5-turbo: $0.50 input / $1.50 output
# - gpt-4-turbo: $10.00 input / $30.00 output